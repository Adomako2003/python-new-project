# area of a rectangle

# find the area of the rectangle

length = float(input("please enter the length "))

breadth = float(input("please enter the breadth"))

# calculate the area with this formula

area = length * breadth

# display the results
print("Area", area )


# A program that accept the arguments length and width and returns it as
# area and parameter of a rectangle

def area(length,breadth):
    return length * breadth

# request for user inputs

def main():
    rec_length = float(input("Enter the length: "))
    rec_breadth = float(input("Enter the breadth: "))

# formula for calculating the area of a rectangle

    area = length * breadth

# print the output or the area of rectangle

    print(f"The area of the rectangle is: {area}")

main()
